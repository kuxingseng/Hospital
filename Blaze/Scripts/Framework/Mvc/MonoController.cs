namespace Blaze.Framework.Mvc
{
    using UnityEngine;

    /// <summary>
    /// 使用<see cref="MonoBehaviour"/>实现的控制器。
    /// </summary>
    public class MonoController : MonoBehaviour
    {
        protected virtual void Awake()
        {
        }

        protected virtual void Start()
        {
        }
    }
}